//let's add the connection string, and let's call it DefaultConnection
{
  "ConnectionStrings": {
    "DefaultConnection": "server=(localdb)\\ProjectModels; database=PizzaApp;"
  },


  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
//connection string - localdb)\\ProjectModels
//The default connection string consists of many attributes, but some of them are really necessary.
//First one is server. Server is equals to local db and this is local db
//DataBase name - PizzaApp    database=PizzaApp 
// "server=(localdb)\\ProjectModels; database=PizzaApp;"
//And this is where we will connect or how we will connect to our database.
  /* Now, in order to do this, we go to a program.cs.Before we add the first service, add raise of pages.
We will add our database application, ApplicationDbContext and configure it in that way that it will connect to
the connection string that we have just created.*/
